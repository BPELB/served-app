import React from 'react'
import ReactDOM from 'react-dom/client'
import ServedApp from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ServedApp />
  </React.StrictMode>
)
